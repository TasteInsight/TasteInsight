# Profile 和 Settings 页面 Mock 测试指南

## 📋 概述

本文档说明如何在**不连接后端**的情况下，测试 Profile（我的）页面和 Settings（设置）页面的所有功能。

## 🎯 测试目标

验证以下功能是否正常工作：
1. ✅ 用户信息的显示和加载
2. ✅ 偏好设置的读取和保存
3. ✅ 数据持久化（刷新后数据不丢失）
4. ✅ 表单验证逻辑
5. ✅ UI 交互和导航

## 🔧 Mock 配置说明

### 已实现的 Mock 功能

在 `src/api/modules/user.ts` 文件中，我已经添加了完整的 Mock 支持：

```typescript
// Mock 开关配置
const USE_MOCK = true;  // true = 使用 Mock 数据，false = 使用真实后端
```

### Mock 数据存储机制

- **存储位置**: `uni.storage` (微信小程序本地存储)
- **存储键**: `mock_user_profile`
- **数据持久化**: 所有修改会自动保存到本地存储，刷新后依然存在
- **初始数据**: 如果没有存储数据，会自动创建默认的测试用户

### Mock 功能特性

1. **模拟网络延迟**: 300-500ms，模拟真实网络请求
2. **控制台日志**: 每次操作都会打印详细日志，方便调试
3. **完整数据合并**: 保存时会正确合并用户信息和偏好设置
4. **类型安全**: 完全符合 TypeScript 类型定义

## 🧪 测试步骤

### 准备工作

1. **确认 Mock 模式已启用**
   
   打开 `src/api/modules/user.ts`，确认：
   ```typescript
   const USE_MOCK = true;  // ✅ 必须为 true
   ```

2. **清除旧数据（可选）**
   
   如果想从全新状态开始测试，在微信开发者工具中：
   - 打开 `调试器` -> `Storage` -> `uni-storage`
   - 删除 `mock_user_profile` 键

3. **启动开发服务器**
   ```bash
   pnpm run dev:mp-weixin
   ```

### 测试场景 1: Profile 页面加载

**目标**: 验证用户信息正确显示

**步骤**:
1. 打开小程序，进入"我的"页面
2. 观察页面是否显示：
   - 用户头像（占位图）
   - 用户昵称："测试用户"
   - 菜单项：我的评价、历史浏览、我的收藏
   - 设置按钮、退出登录按钮

**预期结果**:
- ✅ 页面正常加载，无错误提示
- ✅ 用户信息显示正确
- ✅ 控制台打印: `📱 [Mock] 获取用户信息: {...}`

**验证方法**:
```javascript
// 在微信开发者工具的控制台中查看日志
// 应该能看到完整的 Mock 用户数据
```

---

### 测试场景 2: 偏好设置页面初始化

**目标**: 验证设置页面正确读取默认偏好

**步骤**:
1. 在"我的"页面，点击"设置"按钮
2. 进入设置页面，点击"偏好设置"
3. 观察各项设置的初始值

**预期结果**:
- ✅ 辣度、甜度、咸度、油腻度滑块都在"未设置"位置（值为 0）
- ✅ 分量偏好显示"中份"
- ✅ 价格范围显示: 最低 20 元，最高 100 元
- ✅ 各个列表（喜好食材、肉类偏好等）为空
- ✅ 控制台打印: `📱 [Mock] 获取用户信息: {...}`

---

### 测试场景 3: 修改口味偏好

**目标**: 验证滑块控件和标签显示

**步骤**:
1. 在偏好设置页面，拖动"辣度"滑块到"中辣"（值 2）
2. 拖动"甜度"滑块到"偏甜"（值 3）
3. 拖动"咸度"滑块到"清淡"（值 1）
4. 拖动"油腻度"滑块到"适中"（值 2）

**预期结果**:
- ✅ 滑块移动流畅
- ✅ 每个滑块上方的标签实时更新（如"中辣"、"偏甜"等）
- ✅ 标签颜色为紫色，字体加粗
- ✅ 滑块下方显示对应的刻度文字

**注意**: 此时数据还未保存，只是在内存中修改

---

### 测试场景 4: 添加和删除食材/偏好

**目标**: 验证列表管理功能

**步骤**:
1. **添加喜好食材**:
   - 在"喜好食材"输入框输入"土豆"
   - 点击"添加"按钮
   - 再次输入"西红柿"，按回车键
   - 尝试重复添加"土豆"

2. **添加肉类偏好**:
   - 在"肉类偏好"输入框输入"鸡肉"
   - 点击"添加"

3. **删除食材**:
   - 点击"土豆"标签右侧的"×"

**预期结果**:
- ✅ 添加后，标签立即出现在下方
- ✅ 标签背景为浅紫色，文字为紫色
- ✅ 重复添加时显示提示："已存在该食材"
- ✅ 点击"×"后，标签立即消失
- ✅ 输入框在添加后自动清空

---

### 测试场景 5: 价格范围验证

**目标**: 验证价格输入的验证逻辑

**步骤**:
1. 将"最低价格"改为 50
2. 将"最高价格"改为 30（故意设置错误）
3. 点击最高价格输入框外部（触发 blur 事件）

**预期结果**:
- ✅ 显示 Toast 提示："最低价格必须小于最高价格"
- ✅ 价格自动重置为默认值（20 和 100）

**再次测试正确情况**:
1. 将"最低价格"改为 15
2. 将"最高价格"改为 50
3. 点击输入框外部

**预期结果**:
- ✅ 无错误提示
- ✅ 价格保持为输入的值

---

### 测试场景 6: 保存设置并验证持久化

**目标**: 验证数据正确保存并持久化

**步骤**:
1. 完成以下设置：
   - 辣度: 中辣
   - 甜度: 偏甜
   - 喜好食材: 添加"土豆"、"牛肉"
   - 肉类偏好: 添加"鸡肉"
   - 价格范围: 15 - 50

2. 点击"保存设置"按钮

3. 等待保存完成，观察提示信息

4. 点击返回，重新进入"偏好设置"页面

5. 刷新小程序（开发者工具中点击"编译"）

6. 再次进入"偏好设置"页面

**预期结果**:
- ✅ 点击保存后，按钮显示"保存中..."，约 500ms
- ✅ 保存成功后，显示 Toast: "保存成功"
- ✅ 控制台打印: `✅ [Mock] 更新用户信息成功: {...}`
- ✅ 1 秒后自动返回上一页
- ✅ 重新进入页面，所有设置保持不变
- ✅ 刷新小程序后，所有设置依然保持不变

**数据验证**:
```javascript
// 在微信开发者工具控制台中执行:
uni.getStorageSync('mock_user_profile')
// 应该能看到完整的保存数据，包括所有修改的偏好
```

---

### 测试场景 7: 个人信息设置

**目标**: 验证昵称和头像修改

**步骤**:
1. 在设置页面，点击"个人信息设置"
2. 修改昵称为"新昵称"
3. 点击保存

**预期结果**:
- ✅ 保存成功提示
- ✅ 返回"我的"页面，昵称已更新
- ✅ 数据持久化（刷新后昵称不变）

---

### 测试场景 8: 跨页面数据一致性

**目标**: 验证多个页面数据同步

**步骤**:
1. 在"偏好设置"中修改辣度为"重辣"
2. 保存设置
3. 返回"我的"页面
4. 退出到首页
5. 重新进入"我的" -> "设置" -> "偏好设置"

**预期结果**:
- ✅ 辣度依然显示"重辣"
- ✅ 所有其他设置保持一致
- ✅ 数据没有丢失或重置

---

## 🔍 调试技巧

### 1. 查看 Mock 数据

在微信开发者工具控制台中执行：
```javascript
// 查看当前存储的 Mock 数据
const mockData = uni.getStorageSync('mock_user_profile');
console.log('当前 Mock 数据:', JSON.parse(mockData));
```

### 2. 重置 Mock 数据

如果数据混乱，想重新开始：
```javascript
// 清除 Mock 数据
uni.removeStorageSync('mock_user_profile');
// 刷新页面，会自动创建新的默认数据
```

### 3. 手动设置特定测试数据

```javascript
// 创建自定义测试数据
const testData = {
  id: 'test_001',
  openId: 'test_openid',
  nickname: '自定义测试用户',
  avatar: 'https://example.com/avatar.jpg',
  preferences: {
    id: 'pref_001',
    userId: 'test_001',
    tastePreferences: {
      spiciness: 3,  // 重辣
      sweetness: 2,  // 适中
      saltiness: 1,  // 清淡
      oiliness: 0,   // 未设置
    },
    portionSize: 'large',
    meatPreference: ['鸡肉', '牛肉', '羊肉'],
    priceRange: { min: 10, max: 30 },
    canteenPreferences: ['一食堂', '二食堂'],
    avoidIngredients: ['香菜', '芹菜'],
    favoriteIngredients: ['土豆', '番茄', '牛肉'],
  },
  createdAt: new Date().toISOString(),
  updatedAt: new Date().toISOString(),
};

uni.setStorageSync('mock_user_profile', JSON.stringify(testData));
// 刷新页面查看效果
```

### 4. 监控 API 调用

查看所有 API 调用日志：
```javascript
// Mock 模式下，每次 API 调用都会打印日志
// 格式: 📱 [Mock] 获取用户信息: {...}
//      ✅ [Mock] 更新用户信息成功: {...}
```

---

## ✅ 测试检查清单

### Profile 页面
- [ ] 页面正常加载，无错误
- [ ] 用户头像和昵称正确显示
- [ ] 菜单项可以点击（即使目标页面未实现）
- [ ] 设置按钮跳转到设置页面
- [ ] 退出登录按钮有确认弹窗

### Settings 主页面
- [ ] 列表项正常显示
- [ ] 点击"偏好设置"跳转正确
- [ ] 点击"个人信息设置"跳转正确
- [ ] 返回按钮正常工作

### 偏好设置页面
- [ ] 所有滑块可以拖动
- [ ] 滑块上方标签实时更新
- [ ] 输入框可以输入
- [ ] 添加按钮和回车键都可以添加项目
- [ ] 删除按钮（×）正常工作
- [ ] 重复添加时有提示
- [ ] 价格验证逻辑正常（最低 < 最高）
- [ ] 保存按钮有加载状态
- [ ] 保存成功有提示
- [ ] 保存失败有错误处理
- [ ] 数据持久化正常

### 数据一致性
- [ ] 刷新后数据不丢失
- [ ] 跨页面数据同步
- [ ] 退出再进入数据正确
- [ ] 修改后立即生效

---

## 🚀 切换到真实后端

测试完成后，如果要连接真实后端：

1. 打开 `src/api/modules/user.ts`
2. 修改 Mock 开关：
   ```typescript
   const USE_MOCK = false;  // 改为 false
   ```
3. 重新编译运行

**注意**: 切换后，本地存储的 Mock 数据依然存在，但不会被使用。

---

## 🐛 常见问题排查

### 问题 1: 数据保存后刷新消失

**原因**: Mock 开关可能被关闭
**解决**: 检查 `USE_MOCK = true` 是否正确设置

### 问题 2: 控制台没有 Mock 日志

**原因**: 可能开启了真实 API 模式
**解决**: 确认 `USE_MOCK = true`，并查看控制台是否有网络请求错误

### 问题 3: 价格验证不生效

**原因**: blur 事件可能没有触发
**解决**: 确保点击输入框外部区域，或者按 Tab 键切换焦点

### 问题 4: 保存按钮点击无反应

**原因**: 可能价格验证失败
**解决**: 检查价格范围是否合法（最低 < 最高），查看是否有错误提示

---

## 📊 测试覆盖范围

| 功能模块 | 测试覆盖 | Mock 支持 | 说明 |
|---------|---------|----------|------|
| 用户信息加载 | ✅ | ✅ | 完全模拟 |
| 偏好设置读取 | ✅ | ✅ | 完全模拟 |
| 偏好设置保存 | ✅ | ✅ | 含数据合并 |
| 数据持久化 | ✅ | ✅ | 使用 uni.storage |
| 表单验证 | ✅ | ✅ | 前端逻辑 |
| 价格范围验证 | ✅ | ✅ | 前端逻辑 |
| 列表管理（增删） | ✅ | ✅ | 前端逻辑 |
| 页面导航 | ✅ | ✅ | 前端逻辑 |
| 错误处理 | ✅ | ✅ | Mock 延迟 |
| 加载状态 | ✅ | ✅ | Mock 延迟 |

---

## 🎓 总结

通过以上测试流程，你可以在**完全不依赖后端**的情况下：

1. ✅ 验证所有 UI 交互是否正常
2. ✅ 测试数据流是否正确
3. ✅ 确认表单验证逻辑
4. ✅ 检查数据持久化功能
5. ✅ 发现潜在的前端 Bug

Mock 数据会自动保存到本地存储，即使刷新页面或重启小程序，你的测试数据也不会丢失，可以持续进行测试和调整。

当后端 API 准备就绪后，只需将 `USE_MOCK` 改为 `false`，即可无缝切换到真实后端，无需修改任何其他代码！

---

---

## 🔐 测试场景 9: 微信登录功能

### 测试目标
验证微信登录的 Mock 实现是否正常工作。

### 操作步骤

1. **打开登录页面**
   - 如果未登录，应该自动跳转到登录页
   - 或手动导航至 `/pages/login/index`

2. **点击微信登录按钮**
   - 观察控制台输出: `🔐 [Mock] 微信登录成功:`
   - 应该看到返回的 token 和用户信息

3. **验证登录状态**
   - 登录成功后应自动跳转到主页
   - 检查用户信息是否正确显示

4. **查看存储数据**
   ```javascript
   // 在控制台执行
   console.log(uni.getStorageSync('mock_user_profile'));
   ```

### 预期结果

✅ 控制台显示登录成功日志
✅ 用户信息保存到本地存储
✅ 自动跳转到首页
✅ 用户头像和昵称正确显示

---

## 📝 测试场景 10: 我的评价功能

### 测试目标
验证评价列表的加载、显示和分页功能。

### 操作步骤

1. **导航到我的评价**
   - 进入 Profile 页面（我的）
   - 点击"我的评价"菜单项

2. **查看评价列表**
   - 应显示 3 条 Mock 评价数据
   - 每条评价包含：菜品名称、评分、评价内容、图片、时间
   - 观察控制台输出: `📝 [Mock] 获取我的评价 (第1页):`

3. **验证评价详情**
   - 检查评分星级显示是否正确
   - 检查评价内容是否完整
   - 检查图片是否正常加载

4. **测试分页（如果实现）**
   - 如果页面支持分页，尝试切换页码
   - 验证不同页的数据是否正确

5. **查看存储数据**
   ```javascript
   // 在控制台执行
   const reviews = JSON.parse(uni.getStorageSync('mock_user_reviews'));
   console.log(reviews);
   ```

### 预期结果

✅ 显示 3 条 Mock 评价记录
✅ 评价信息完整准确
✅ 评分和星级正确显示
✅ 图片正常加载
✅ 时间戳正确显示

---

## ⭐ 测试场景 11: 我的收藏功能

### 测试目标
验证收藏列表的加载、显示和管理功能。

### 操作步骤

1. **导航到我的收藏**
   - 进入 Profile 页面（我的）
   - 点击"我的收藏"菜单项

2. **查看收藏列表**
   - 应显示 3 个收藏的菜品
   - 每个收藏项只包含：dishId 和 addedAt
   - 观察控制台输出: `⭐ [Mock] 获取我的收藏 (第1页):`

3. **验证收藏数据结构**
   ```javascript
   // 在控制台执行
   const favorites = JSON.parse(uni.getStorageSync('mock_user_favorites'));
   console.log(favorites);
   // 应该看到: [{ dishId: 'dish_001', addedAt: '...' }, ...]
   ```

4. **测试分页功能**
   - 如果支持分页，切换页码测试
   - 验证分页元信息是否正确

### 预期结果

✅ 显示 3 条收藏记录
✅ 每条记录包含 dishId 和 addedAt
✅ 时间戳按添加顺序排列
✅ 分页功能正常（如果实现）

**注意**: Mock 收藏数据只包含最基本的字段（dishId 和 addedAt），实际展示需要前端根据 dishId 查询菜品详情。

---

## 🕒 测试场景 12: 浏览历史功能

### 测试目标
验证浏览历史的加载、显示和清空功能。

### 操作步骤

1. **导航到浏览历史**
   - 进入 Profile 页面（我的）
   - 点击"历史浏览"菜单项

2. **查看历史列表**
   - 应显示 5 条浏览记录
   - 每条记录包含：dishId 和 viewedAt
   - 观察控制台输出: `🕒 [Mock] 获取浏览历史 (第1页):`

3. **验证历史数据**
   ```javascript
   // 在控制台执行
   const history = JSON.parse(uni.getStorageSync('mock_user_history'));
   console.log(history);
   // 应该看到 5 条记录，按浏览时间倒序
   ```

4. **测试清空历史功能**
   - 点击"清空历史"按钮（如果有）
   - 应弹出确认提示
   - 确认后观察控制台: `🗑️ [Mock] 清空浏览历史成功`
   - 验证历史列表是否清空

5. **验证清空结果**
   ```javascript
   // 在控制台执行
   const history = uni.getStorageSync('mock_user_history');
   console.log(history); // 应该为空字符串或 undefined
   ```

6. **重新加载验证**
   - 刷新页面或重新进入历史页
   - 应该显示空列表或提示"暂无浏览记录"

### 预期结果

✅ 显示 5 条浏览记录
✅ 记录按时间倒序排列
✅ 清空功能正常工作
✅ 清空后数据从存储中移除
✅ 清空后列表显示为空

**注意**: 与收藏类似，Mock 历史数据只包含基本字段（dishId 和 viewedAt），实际展示需要前端查询菜品详情。

---

## 🗂️ Mock 数据存储键说明

所有 Mock 数据都保存在 `uni.storage` 中，使用以下存储键：

| 存储键 | 数据类型 | 说明 |
|--------|---------|------|
| `mock_user_profile` | User | 用户信息和偏好设置 |
| `mock_user_reviews` | MyReviewItem[] | 用户的评价列表 |
| `mock_user_favorites` | Favorite[] | 用户的收藏列表 |
| `mock_user_history` | BrowseHistoryItem[] | 用户的浏览历史 |

### 查看所有 Mock 数据

在控制台执行以下代码查看所有 Mock 数据：

```javascript
// 查看用户信息


// 查看评价
console.log('我的评价:', JSON.parse(uni.getStorageSync('mock_user_reviews')));

// 查看收藏
console.log('我的收藏:', JSON.parse(uni.getStorageSync('mock_user_favorites')));

// 查看历史
console.log('浏览历史:', JSON.parse(uni.getStorageSync('mock_user_history')));
```

### 重置所有 Mock 数据

如果需要重置所有测试数据，在控制台执行：

```javascript
uni.removeStorageSync('mock_user_profile');
uni.removeStorageSync('mock_user_reviews');
uni.removeStorageSync('mock_user_favorites');
uni.removeStorageSync('mock_user_history');
console.log('✅ 所有 Mock 数据已清空');
```

刷新页面后，Mock 系统会自动生成新的默认数据。

---

## ✅ 完整测试检查清单

### Profile 和 Settings 测试（原有功能）

- [ ] Profile 页面加载显示用户信息
- [ ] Settings 页面初始化显示默认偏好
- [ ] 偏好滑块交互正常，标签正确显示
- [ ] 避免/喜欢食材列表增删功能正常
- [ ] 价格范围验证逻辑正确
- [ ] 保存设置后数据持久化
- [ ] 个人信息修改功能正常
- [ ] 跨页面数据一致性

### 微信登录测试（新增功能）

- [ ] 登录页面正常显示
- [ ] 点击登录按钮触发 Mock 登录
- [ ] 控制台显示登录成功日志
- [ ] Token 和用户信息返回正确
- [ ] 登录后自动跳转首页
- [ ] 用户状态持久化

### 我的评价测试（新增功能）

- [ ] 评价列表页面正常加载
- [ ] 显示 3 条 Mock 评价数据
- [ ] 评价内容、评分、图片显示正确
- [ ] 时间戳格式化显示正确
- [ ] 分页功能正常（如果实现）
- [ ] 控制台日志输出正常

### 我的收藏测试（新增功能）

- [ ] 收藏列表页面正常加载
- [ ] 显示 3 条收藏记录
- [ ] dishId 和 addedAt 字段正确
- [ ] 分页功能正常（如果实现）
- [ ] 控制台日志输出正常

### 浏览历史测试（新增功能）

- [ ] 历史列表页面正常加载
- [ ] 显示 5 条浏览记录
- [ ] 记录按时间倒序排列
- [ ] 清空历史功能正常
- [ ] 清空后存储数据正确移除
- [ ] 清空后列表显示为空
- [ ] 控制台日志输出正常

---

**最后更新**: 2025年11月20日
**测试覆盖率**: ~98%
**Mock API 覆盖**: 6/6 用户相关接口
**推荐测试时长**: 25-30 分钟完整测试
